using System.IO;

namespace Waterfall.System.Instalation
{
    public static class CreateSystemFiles
    {
        public static void CreateFiles(string Path)
        {
            /*
			  Deleted in Core version
		    */
        }
    }
}
